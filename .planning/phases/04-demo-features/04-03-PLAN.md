---
phase: 04-demo-features
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - src/components/build-ui.tsx
  - src/components/role-views/marketer-view.tsx
  - src/components/role-views/qa-view.tsx
  - src/components/role-views/developer-view.tsx
  - src/components/audit-trail.tsx
  - src/components/__tests__/role-views.test.tsx
autonomous: true
requirements:
  - ROLE-01
  - ROLE-02
  - ROLE-03
  - INTEG-02

must_haves:
  truths:
    - "Role toggle switches between Marketer, QA, and Developer views with meaningfully different content"
    - "Marketer view shows brief + chat panel + simplified score badge"
    - "QA view shows full compliance sidebar + audit trail log"
    - "Developer view shows generated PageSpec JSON code output + component spec list"
    - "Audit trail displays timestamped log entries from Convex"
  artifacts:
    - path: "src/components/role-views/marketer-view.tsx"
      provides: "Marketer view with chat-focused layout"
    - path: "src/components/role-views/qa-view.tsx"
      provides: "QA view with full compliance + audit trail"
    - path: "src/components/role-views/developer-view.tsx"
      provides: "Developer view with code output"
    - path: "src/components/audit-trail.tsx"
      provides: "Convex-subscribed audit log panel"
    - path: "src/components/build-ui.tsx"
      provides: "Role toggle state and conditional rendering"
      contains: "role"
  key_links:
    - from: "src/components/audit-trail.tsx"
      to: "convex/auditLog.ts"
      via: "useQuery(api.auditLog.getRecentLogs)"
      pattern: "useQuery.*getRecentLogs"
    - from: "src/components/build-ui.tsx"
      to: "src/components/role-views/"
      via: "role state conditional rendering"
      pattern: "activeRole"
    - from: "src/components/role-views/qa-view.tsx"
      to: "src/components/audit-trail.tsx"
      via: "AuditTrail component inclusion"
      pattern: "AuditTrail"
---

<objective>
Add role-based views (Marketer, QA, Developer) with a role toggle to BuildUI, and build the Convex-subscribed audit trail panel.

Purpose: Role views (ROLE-01/02/03) demonstrate that different stakeholders see tailored experiences from the same underlying data. The audit trail (INTEG-02) gives QA complete visibility into every AI decision.

Output: Three distinct role views accessible via toggle, with audit trail visible in QA view.
</objective>

<execution_context>
@/Users/ved/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ved/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-demo-features/04-RESEARCH.md
@.planning/phases/04-demo-features/04-01-SUMMARY.md

@src/components/build-ui.tsx
@src/components/compliance-sidebar.tsx
@src/components/chat-panel.tsx
@src/components/explainability-panel.tsx
@convex/auditLog.ts

<interfaces>
<!-- Key contracts from Plan 01 that this plan consumes. -->

From src/components/chat-panel.tsx (created in 04-01):
```typescript
// ChatPanel props: { currentSpec, onEditComplete }
export function ChatPanel({ currentSpec, onEditComplete }: ChatPanelProps): JSX.Element;
```

From src/components/explainability-panel.tsx (created in 04-01):
```typescript
// ExplainabilityPanel props: { rawVariants, selectedVariant }
export function ExplainabilityPanel({ rawVariants, selectedVariant }: ExplainabilityPanelProps): JSX.Element;
```

From src/components/compliance-sidebar.tsx:
```typescript
export function ComplianceSidebar({ spec, previewRef, onAutoFix }: { spec: PageSpec | null; previewRef: RefObject<HTMLDivElement | null>; onAutoFix: (spec: PageSpec) => void; }): JSX.Element;
```

From convex/auditLog.ts:
```typescript
export const getRecentLogs = query({ args: {}, handler: ... });
// Returns: Array<{ _id, action, details, timestamp, actor }>
```

From src/types/page-spec.ts:
```typescript
export type PageSpec = { id, title, market, product, sections[], metadata };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build AuditTrail component and three role view components</name>
  <files>src/components/__tests__/role-views.test.tsx, src/components/audit-trail.tsx, src/components/role-views/marketer-view.tsx, src/components/role-views/qa-view.tsx, src/components/role-views/developer-view.tsx</files>
  <behavior>
    - AuditTrail renders "No audit events yet" when log entries array is empty
    - AuditTrail renders timestamped entries when log data is provided
    - AuditTrail shows "Loading audit trail..." when query returns undefined
    - MarketerView renders a score badge with correct color coding (green >80, amber >60, red otherwise)
    - QAView renders AuditTrail and ComplianceSidebar
    - DeveloperView renders JSON.stringify output of currentSpec in a pre/code block
    - DeveloperView shows "Generate a page to see the code output" when currentSpec is null
  </behavior>
  <action>
    0. Write test stubs FIRST (RED): Create src/components/__tests__/role-views.test.tsx with tests for all behavior cases above. Mock Convex useQuery for AuditTrail tests. Mock ComplianceSidebar and ChatPanel as simple stubs. Run tests — they must FAIL before proceeding to implementation.

    1. Create src/components/audit-trail.tsx ("use client"):
       - Import `useQuery` from "convex/react" and `api` from convex/_generated/api
       - Subscribe to `api.auditLog.getRecentLogs`
       - Render as a scrollable list of timestamped entries
       - Each entry: time (toLocaleTimeString), action name (bold), details (truncated, gray)
       - Empty state: "No audit events yet. Generate a page to see AI decisions logged here."
       - Loading state: "Loading audit trail..."
       - Handle case where Convex is not connected (useQuery returns undefined indefinitely) — show graceful message after 3s

    2. Create src/components/role-views/marketer-view.tsx ("use client"):
       - Props: `{ brief, setBrief, onSubmit, canSubmit, buttonLabel, interpretation, currentSpec, onEditComplete, score }`
       - Left column content: brief form + ChatPanel (when spec exists)
       - Right column content: simplified score badge only (overall score as large number with color: green >80, amber >60, red otherwise)
       - Does NOT render full ComplianceSidebar or explainability — marketer sees minimal compliance info

    3. Create src/components/role-views/qa-view.tsx ("use client"):
       - Props: `{ currentSpec, previewRef, onAutoFix, gateViolations }`
       - Left column content: full violations list (extracted from ComplianceSidebar or rendered separately), AuditTrail panel below
       - Right column content: ComplianceSidebar (full)
       - This is the power-user compliance view

    4. Create src/components/role-views/developer-view.tsx ("use client"):
       - Props: `{ currentSpec, rawVariants, selectedVariant }`
       - Left column content: component spec list — for each component in currentSpec, show componentId + prop keys
       - Right column content: `<pre><code>` block with `JSON.stringify(currentSpec, null, 2)` — the raw PageSpec JSON
       - If no spec: "Generate a page to see the code output"

    5. Run tests again (GREEN) — all must pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/role-views.test.tsx --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    - All role-views tests pass (RED then GREEN cycle completed)
    - AuditTrail subscribes to Convex getRecentLogs and renders entries
    - MarketerView shows brief + chat + simplified score
    - QAView shows violations + audit trail + full sidebar
    - DeveloperView shows JSON code output + component spec list
    - All components compile cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire role toggle into BuildUI with conditional column content</name>
  <files>src/components/build-ui.tsx</files>
  <action>
    1. Add role state to BuildUI: `useState<'marketer' | 'qa' | 'developer'>('marketer')`

    2. Add role toggle UI at the top of BuildUI (above the 3-column grid, below the header):
       - Three buttons: "Marketer", "QA", "Developer"
       - Active tab styled with pfizer-blue-700 bg, inactive with border
       - Compact horizontal bar, not taking much vertical space

    3. Keep the 3-column grid structure CONSTANT across all roles (per research pitfall 5). The center column (preview) is always the same. Only left and right column content changes based on role.

    4. Conditional rendering by role:
       - **Marketer** (default):
         - Left: brief form + ChatPanel (existing) + error/gate banners
         - Right: simplified score badge (extract from ComplianceSidebar's computeScore, render as large number)
       - **QA**:
         - Left: ComplianceSidebar violations detail + AuditTrail panel (scrollable)
         - Right: Full ComplianceSidebar component (as currently wired)
       - **Developer**:
         - Left: Component spec list from currentSpec
         - Right: `<pre>` code block with PageSpec JSON

    5. Import and render the role view components. Pass them the necessary props from BuildUI state. The role views provide the left and right column CONTENT — they do not create their own grid. BuildUI still owns the grid layout.

    6. Alternative approach (simpler): Instead of separate role view components providing column content, use inline conditional rendering in BuildUI itself. If the role view components add too much indirection, keep the logic inline. The key requirement is that the grid structure stays constant and only content swaps.

    7. Ensure ExplainabilityPanel (from Plan 01) is visible in Marketer view below the preview (center column) — all roles can see the reasoning, but only marketer gets it prominently.

    8. All existing BuildUI functionality (submit, variant tabs, gate errors, auto-fix) MUST continue working in all roles. Role switching is purely a view concern.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=dot && npm run build</automated>
  </verify>
  <done>
    - Role toggle renders 3 buttons above the grid
    - Marketer view: brief + chat on left, simplified score on right
    - QA view: violations + audit trail on left, full compliance sidebar on right
    - Developer view: component specs on left, JSON code on right
    - Center preview column identical across all roles
    - All existing pipeline functionality works in every role
    - Build succeeds, all tests pass
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npx vitest run --reporter=verbose` — all tests pass
3. Toggle to Marketer: see brief + chat + score badge
4. Toggle to QA: see violations + audit trail + full sidebar
5. Toggle to Developer: see JSON code + component specs
6. Generate a page, switch roles — preview stays, only chrome changes
7. Audit trail shows log entries (if Convex is connected)
</verification>

<success_criteria>
- Role toggle switches views with no layout reflow
- Each role shows meaningfully different content per requirements ROLE-01/02/03
- Audit trail displays Convex log entries in real-time
- All existing pipeline features work across all roles
</success_criteria>

<output>
After completion, create `.planning/phases/04-demo-features/04-03-SUMMARY.md`
</output>
