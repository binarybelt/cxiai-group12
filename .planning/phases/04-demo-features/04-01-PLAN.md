---
phase: 04-demo-features
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/build/page.tsx
  - src/components/build-ui.tsx
  - src/app/api/chat-edit/route.ts
  - src/agents/chat-editor/prompt.md
  - src/lib/diff.ts
  - src/types/diff.ts
  - src/components/chat-panel.tsx
  - src/components/explainability-panel.tsx
  - src/lib/__tests__/diff.test.ts
autonomous: true
requirements:
  - BUILD-05
  - BUILD-06

must_haves:
  truths:
    - "User types a chat edit instruction and the page updates with a visible diff summary"
    - "Explainability panel shows why each component was chosen (selectionReason from constrained schema)"
    - "Build page fills the viewport correctly with no overflow or broken grid"
  artifacts:
    - path: "src/app/build/page.tsx"
      provides: "Fixed layout wrapper with h-screen flex-col"
      contains: "h-screen"
    - path: "src/app/api/chat-edit/route.ts"
      provides: "POST endpoint for chat-edit using generateObject"
      exports: ["POST"]
    - path: "src/lib/diff.ts"
      provides: "computePageSpecDiff function"
      exports: ["computePageSpecDiff"]
    - path: "src/components/chat-panel.tsx"
      provides: "Chat input with message history and diff display"
    - path: "src/components/explainability-panel.tsx"
      provides: "Per-component selectionReason list"
  key_links:
    - from: "src/components/chat-panel.tsx"
      to: "/api/chat-edit"
      via: "fetch POST"
      pattern: "fetch.*api/chat-edit"
    - from: "src/components/build-ui.tsx"
      to: "src/components/explainability-panel.tsx"
      via: "rawSpec state prop"
      pattern: "rawSpec"
    - from: "src/components/build-ui.tsx"
      to: "src/components/chat-panel.tsx"
      via: "currentSpec + onEdit callback"
      pattern: "ChatPanel"
---

<objective>
Fix the /build page layout bug, add chat-to-edit with diff display, and add explainability panel showing component selection reasoning.

Purpose: The layout fix is prerequisite for all Phase 4 UI work. Chat-to-edit (BUILD-05) is the demo's core interactive loop. Explainability (BUILD-06) surfaces the AI reasoning already captured in selectionReason fields.

Output: Working chat-edit flow with diff summary, explainability panel, and a properly laid out 3-column build page.
</objective>

<execution_context>
@/Users/ved/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ved/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-demo-features/04-RESEARCH.md

@src/app/build/page.tsx
@src/components/build-ui.tsx
@src/app/api/generate-page/route.ts
@src/agents/component-selector/schema.ts
@src/types/page-spec.ts
@src/lib/compliance.ts
@src/lib/convex-client.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/page-spec.ts:
```typescript
export type PageSpec = z.infer<typeof PageSpecSchema>;
// PageSpec has: id, title, market, product, sections[], metadata
// Section has: id, type, components[], order
// ComponentRef has: componentId, props, tokenOverrides?
```

From src/agents/component-selector/schema.ts:
```typescript
export function buildConstrainedPageSpecSchema(): z.ZodObject<...>;
export type ConstrainedPageSpecOutput = { variants: Array<PageSpec & { sections: Array<Section & { components: Array<ComponentRef & { selectionReason: string }> }> }> };
// selectionReason is on each component ref in the constrained output
```

From src/lib/compliance.ts:
```typescript
export function runComplianceGate(spec: PageSpec): { passed: boolean; violations: ComplianceViolation[] };
```

From src/lib/convex-client.ts:
```typescript
export function logGeneration(action: string, details: string): void;
// Fire-and-forget audit log via Convex mutation
```

From src/lib/llm.ts:
```typescript
export function getLLM(): LanguageModel;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix layout, create diff types/logic, create chat-edit API route</name>
  <files>src/app/build/page.tsx, src/types/diff.ts, src/lib/diff.ts, src/lib/__tests__/diff.test.ts, src/app/api/chat-edit/route.ts, src/agents/chat-editor/prompt.md</files>
  <behavior>
    - computePageSpecDiff(oldSpec, newSpec) returns { summary, addedSections, removedSections, modifiedComponents }
    - When sections are added, addedSections contains their IDs
    - When sections are removed, removedSections contains their IDs
    - When component props change, modifiedComponents contains { sectionId, componentId, what: "props changed" }
    - When specs are identical, summary is "No visible changes"
  </behavior>
  <action>
    1. Fix build/page.tsx layout: wrap content in `<div className="flex h-screen flex-col overflow-hidden">`, header gets `flex-shrink-0`, BuildUI will get flex-1 in Task 2.

    2. Create src/types/diff.ts with DiffResult interface:
       ```typescript
       export interface DiffResult {
         summary: string;
         addedSections: string[];
         removedSections: string[];
         modifiedComponents: Array<{ sectionId: string; componentId: string; what: string }>;
       }
       ```

    3. Create src/lib/diff.ts with `computePageSpecDiff(oldSpec: PageSpec, newSpec: PageSpec): DiffResult`. Compare section IDs for adds/removes. For shared sections, compare component props via JSON.stringify. Build summary string from counts.

    4. TDD: Write tests in src/lib/__tests__/diff.test.ts FIRST (RED), then implement (GREEN). Test cases: identical specs, added section, removed section, modified component props, multiple changes.

    5. Create src/agents/chat-editor/prompt.md — system prompt for chat-edit agent. Instruct: "You are editing an existing pharma page spec. The user wants to modify the page. You receive the current PageSpec and an edit instruction. Return a complete new PageSpec (not a delta). Preserve all sections the user did not mention. Apply the edit faithfully. Keep all compliance requirements (disclaimers, ISI, adverse event URLs)."

    6. Create src/app/api/chat-edit/route.ts — POST endpoint:
       - Accept `{ spec: PageSpec, instruction: string }` in body
       - Use `generateObject` with `buildConstrainedPageSpecSchema()` (reuse from component-selector)
       - Read prompt from agents/chat-editor/prompt.md
       - Inject current spec JSON into system prompt
       - Run `runComplianceGate` on result variant
       - Return `{ spec: result.object.variants[0], violations }` on success
       - Return 422 with violations if gate fails (same pattern as generate-page)
       - Fire-and-forget `logGeneration("chat-edit", ...)` for audit trail
       - NOTE: The schema generates 2 variants but chat-edit only needs one — return `variants[0]`
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/diff.test.ts --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    - build/page.tsx uses h-screen flex-col layout
    - computePageSpecDiff passes all tests (add, remove, modify, identical)
    - /api/chat-edit route compiles and follows generate-page pattern
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Build ChatPanel, ExplainabilityPanel, and wire into BuildUI</name>
  <files>src/components/chat-panel.tsx, src/components/explainability-panel.tsx, src/components/build-ui.tsx</files>
  <action>
    1. Create src/components/chat-panel.tsx ("use client"):
       - Props: `{ currentSpec: PageSpec | null; onEditComplete: (newSpec: PageSpec, diff: DiffResult) => void }`
       - State: `chatHistory: ChatMessage[]` where `ChatMessage = { role: "user" | "assistant"; content: string }`
       - State: `isEditing: boolean`, `input: string`
       - On submit: POST to /api/chat-edit with `{ spec: currentSpec, instruction: input }`
       - On success: call `computePageSpecDiff(currentSpec, newSpec)`, call `onEditComplete(newSpec, diff)`
       - Add user message and assistant summary to chatHistory
       - On 422: show inline error "Edit blocked — compliance violations found"
       - UI: scrollable message list + input bar at bottom. Messages styled as chat bubbles. Diff summary shown as assistant message.

    2. Create src/components/explainability-panel.tsx ("use client"):
       - Props: `{ rawVariants: any[] | null; selectedVariant: number }`
       - Iterates rawVariants[selectedVariant].sections, then each section's components
       - For each component: renders componentId + selectionReason in a compact list
       - If rawVariants is null, show "Generate a page to see AI reasoning"
       - Accepts `any[]` because the raw constrained output includes selectionReason which is not in the base PageSpec type — use runtime access

    3. Update src/components/build-ui.tsx:
       - Add `rawSpec` state: `useState<any>(null)` — stores the raw constrained output from generate-page (preserves selectionReason)
       - Modify generate-page response handling: the response already returns the full constrained output (variants with selectionReason). Store it in rawSpec: `setRawSpec(specResult)`.
       - Add `diffResult` state: `useState<DiffResult | null>(null)`
       - Add `handleChatEdit` callback: receives newSpec + diff from ChatPanel, updates variants[0] with newSpec (immutable), sets diffResult, clears overrideSpec
       - Update outer div to `className="flex flex-1 overflow-hidden"` and inner to `lg:grid lg:grid-cols-[1fr_2fr_320px]`
       - Each column gets `overflow-y-auto`
       - Add ChatPanel below the brief form in the left column (only shown when currentSpec exists)
       - Add ExplainabilityPanel below the preview in the center column (collapsible accordion)
       - Add diff summary banner above preview when diffResult is set (dismissible)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=dot</automated>
  </verify>
  <done>
    - ChatPanel renders with input, sends edits to /api/chat-edit, shows chat history
    - ExplainabilityPanel renders selectionReason for each component from rawSpec
    - BuildUI uses flex-1 overflow-hidden layout with all 3 columns scrollable
    - Diff summary banner appears after chat edit
    - All existing tests still pass
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npx vitest run --reporter=verbose` — all tests pass including new diff tests
3. Layout check: /build page fills viewport, no horizontal scroll, all 3 columns visible
4. Chat-edit: type instruction after generating a page, page updates, diff summary shows
5. Explainability: after generation, selectionReason visible for each component
</verification>

<success_criteria>
- Build page fills viewport with h-screen flex-col layout (no overflow bug)
- User can type "make it warmer" after generation and receive an updated page with diff summary
- Explainability panel shows selectionReason for every component in the generated spec
- All existing + new tests pass
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-demo-features/04-01-SUMMARY.md`
</output>
