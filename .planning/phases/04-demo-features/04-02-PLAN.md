---
phase: 04-demo-features
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/scan/page.tsx
  - src/components/scan-dashboard.tsx
  - src/app/api/scan-url/route.ts
  - src/lib/scan.ts
  - src/types/scan.ts
  - src/design-system/portfolio.json
  - src/lib/figma.ts
  - src/components/figma-import.tsx
  - src/lib/__tests__/scan.test.ts
  - src/lib/__tests__/figma.test.ts
autonomous: true
requirements:
  - SCAN-01
  - SCAN-02
  - INTEG-01

must_haves:
  truths:
    - "SCAN dashboard displays pre-loaded portfolio compliance overview with status indicators"
    - "User can enter a URL and receive a drift report showing off-brand colors"
    - "User can paste a Figma URL and see extracted design tokens"
  artifacts:
    - path: "src/app/scan/page.tsx"
      provides: "SCAN dashboard route"
    - path: "src/lib/scan.ts"
      provides: "driftReport function for URL scanning"
      exports: ["driftReport"]
    - path: "src/components/scan-dashboard.tsx"
      provides: "Portfolio overview + live scan form"
    - path: "src/lib/figma.ts"
      provides: "extractTokensFromFigmaUrl function"
      exports: ["extractTokensFromFigmaUrl"]
    - path: "src/components/figma-import.tsx"
      provides: "Figma URL paste + token preview panel"
    - path: "src/design-system/portfolio.json"
      provides: "Pre-seeded portfolio data for SCAN-01"
  key_links:
    - from: "src/components/scan-dashboard.tsx"
      to: "/api/scan-url"
      via: "fetch POST for live URL scan"
      pattern: "fetch.*api/scan-url"
    - from: "src/app/api/scan-url/route.ts"
      to: "src/lib/scan.ts"
      via: "driftReport function call"
      pattern: "driftReport"
    - from: "src/components/figma-import.tsx"
      to: "src/lib/figma.ts"
      via: "extractTokensFromFigmaUrl call"
      pattern: "extractTokensFromFigmaUrl"
---

<objective>
Build the SCAN dashboard with portfolio overview and live URL drift scanning, plus Figma token import panel.

Purpose: SCAN-01/02 demonstrate portfolio-wide compliance monitoring (a key differentiator). INTEG-01 shows Figma design system integration. Both are self-contained features on independent routes/panels.

Output: Working /scan route with portfolio cards and live URL scan, plus a Figma import component ready for integration into role views.
</objective>

<execution_context>
@/Users/ved/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ved/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-demo-features/04-RESEARCH.md

@src/design-system/tokens.json
@src/lib/design-system.ts
@src/types/design-system.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/design-system.ts:
```typescript
export interface DesignToken { id: string; name: string; value: string; category: string; }
```

From src/lib/design-system.ts:
```typescript
export function loadTokens(): DesignToken[];
export function loadComponents(): DesignComponent[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SCAN types, drift report logic, and portfolio data</name>
  <files>src/types/scan.ts, src/lib/scan.ts, src/lib/__tests__/scan.test.ts, src/design-system/portfolio.json, src/app/api/scan-url/route.ts</files>
  <behavior>
    - driftReport given HTML with off-brand hex values returns driftItems with those hex values
    - driftReport given HTML with only approved hex values returns zero driftItems
    - driftReport returns url, driftCount, items array, and scannedAt timestamp
    - Portfolio JSON contains 6 entries with url, product, market, complianceScore, lastScanned, driftCount, status
  </behavior>
  <action>
    1. Create src/types/scan.ts:
       ```typescript
       export interface DriftItem { hex: string; message: string; }
       export interface ScanReport { url: string; driftCount: number; items: DriftItem[]; scannedAt: string; }
       export interface PortfolioEntry { url: string; product: string; market: string; complianceScore: number; lastScanned: string; driftCount: number; status: "compliant" | "warning" | "critical"; }
       ```

    2. TDD: Write src/lib/__tests__/scan.test.ts FIRST. Test driftReport with mock HTML containing hex values. Test that approved hex values pass, off-brand hex values are flagged. Then implement.

    3. Create src/lib/scan.ts with `driftReport(url: string, html: string, approvedTokens: DesignToken[]): ScanReport`. Note: accept html as parameter (pre-fetched) so the function is pure and testable. Extract hex values via regex `/#[0-9a-fA-F]{3,8}/g`. Filter against approved color tokens. Return ScanReport.

    4. Create src/design-system/portfolio.json — 6 fabricated Pfizer portfolio entries:
       - Lipitor US HCP (compliant, score 95)
       - Eliquis EU Patient (warning, score 72, 3 drift items)
       - Prevnar UK HCP (compliant, score 88)
       - Ibrance US Patient (critical, score 45, 8 drift items)
       - Xeljanz JP HCP (warning, score 68, 5 drift items)
       - Comirnaty Global Patient (compliant, score 92)

    5. Create src/app/api/scan-url/route.ts — POST endpoint:
       - Accept `{ url: string }` in body
       - Validate URL format
       - Server-side fetch the URL HTML (try/catch for network errors)
       - Call `driftReport(url, html, loadTokens())`
       - Return ScanReport JSON
       - On fetch error, return 422 with `{ error: "Could not fetch URL. Ensure it is publicly accessible." }`
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/scan.test.ts --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    - driftReport correctly identifies off-brand hex values (tests pass)
    - portfolio.json contains 6 entries with realistic data
    - /api/scan-url route compiles and handles fetch errors gracefully
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build SCAN dashboard UI, Figma import logic and component</name>
  <files>src/app/scan/page.tsx, src/components/scan-dashboard.tsx, src/lib/figma.ts, src/lib/__tests__/figma.test.ts, src/components/figma-import.tsx</files>
  <behavior>
    - extractTokensFromFigmaUrl parses file key from standard Figma URLs (figma.com/file/KEY, figma.com/design/KEY)
    - extractTokensFromFigmaUrl throws on invalid URLs
    - extractTokensFromFigmaUrl returns demo fallback tokens when FIGMA_API_TOKEN is not set
  </behavior>
  <action>
    1. TDD: Write src/lib/__tests__/figma.test.ts FIRST. Test URL parsing for figma.com/file/ABC and figma.com/design/XYZ patterns. Test invalid URL throws. Test demo fallback returns tokens. Then implement.

    2. Create src/lib/figma.ts:
       - `extractTokensFromFigmaUrl(url: string): Promise<TokenDraft[]>` where `TokenDraft = { name: string; value: string; category: "color" }`
       - Parse file key from URL via regex: `/figma\.com\/(?:file|design)\/([a-zA-Z0-9]+)/`
       - If `FIGMA_API_TOKEN` env var exists: fetch `https://api.figma.com/v1/files/{fileKey}` with `X-Figma-Token` header, extract color fills from document nodes
       - If no token: return `DEMO_FIGMA_TOKENS` — 8 hardcoded color tokens resembling a pharma brand palette (blues, grays, accent colors)
       - Export `DEMO_FIGMA_TOKENS` for testing

    3. Create src/components/figma-import.tsx ("use client"):
       - URL input + "Import" button
       - On submit: call extractTokensFromFigmaUrl (client-side for demo fallback, or create a thin API route if needed for server-side Figma API)
       - Display extracted tokens as a color swatch grid with hex values
       - Show side-by-side comparison: "Extracted" vs "Approved Palette" (load approved from design-system)
       - Loading and error states

    4. Create src/components/scan-dashboard.tsx ("use client"):
       - Load portfolio.json (static import)
       - Render portfolio cards: product name, market, compliance score with color coding (green/amber/red), drift count, last scanned date
       - Live scan section: URL input + "Scan" button
       - On scan: POST to /api/scan-url, display ScanReport results (drift items list)
       - Pre-fill URL input with a working example URL
       - Loading and error states

    5. Create src/app/scan/page.tsx — server component:
       - Page header "SCAN — Portfolio Compliance Monitor"
       - Render ScanDashboard component
       - Include FigmaImport component below the scan dashboard in a collapsible section
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/figma.test.ts --reporter=verbose && npx tsc --noEmit && npx vitest run --reporter=dot</automated>
  </verify>
  <done>
    - /scan page renders with portfolio cards showing compliance status
    - Live URL scan form submits and displays drift report
    - Figma import parses URL, shows extracted tokens (demo fallback works without API key)
    - All tests pass, TypeScript compiles
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npx vitest run --reporter=verbose` — all tests pass including scan and figma tests
3. Navigate to /scan — portfolio cards render with color-coded compliance scores
4. Enter a URL in scan form — drift report appears
5. Enter a Figma URL in import form — tokens appear as color swatches
</verification>

<success_criteria>
- /scan page displays 6 portfolio entries with compliance scores and status badges
- Live URL scan returns drift report with off-brand color items
- Figma import shows extracted tokens (demo fallback when no API key)
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-demo-features/04-02-SUMMARY.md`
</output>
