---
phase: 03-comply-engine
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/axe-scanner.ts
  - src/lib/__tests__/axe-scanner.test.ts
  - src/app/api/generate-page/route.ts
autonomous: true
requirements: [COMPLY-03, COMPLY-06]

must_haves:
  truths:
    - "axe-core scans rendered DOM and returns WCAG AA violations"
    - "axe scanner wraps violations into a typed A11yViolation array"
    - "Compliance gate in generate-page route validates completed PageSpec and returns 422 on failure"
  artifacts:
    - path: "src/lib/axe-scanner.ts"
      provides: "Browser-only axe-core wrapper for WCAG AA scanning"
      exports: ["scanForA11yViolations", "A11yViolation"]
    - path: "src/lib/__tests__/axe-scanner.test.ts"
      provides: "Unit tests for axe scanner in jsdom"
      min_lines: 30
    - path: "src/app/api/generate-page/route.ts"
      provides: "Compliance gate wired before render response"
      contains: "runComplianceGate"
  key_links:
    - from: "src/lib/axe-scanner.ts"
      to: "axe-core"
      via: "dynamic import"
      pattern: "import.*axe-core"
    - from: "src/app/api/generate-page/route.ts"
      to: "src/lib/compliance.ts"
      via: "runComplianceGate import"
      pattern: "import.*runComplianceGate.*from.*compliance"
---

<objective>
Build the axe-core accessibility scanner wrapper and wire the compliance gate into the generate-page API route.

Purpose: COMPLY-03 requires WCAG AA accessibility scanning against rendered DOM. COMPLY-06 requires the pipeline to block rendering when the PageSpec fails compliance. This plan delivers both: the scanner as a browser-only module, and the gate as a server-side check in the API route.

Output: `src/lib/axe-scanner.ts` (axe wrapper), `src/lib/__tests__/axe-scanner.test.ts` (tests), modified `src/app/api/generate-page/route.ts` (gate)
</objective>

<execution_context>
@/Users/ved/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ved/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-comply-engine/03-RESEARCH.md

@src/app/api/generate-page/route.ts
@src/types/page-spec.ts

<interfaces>
<!-- Key types from Plan 01 that this plan will import (created in parallel wave) -->
<!-- For the gate wiring, this plan imports runComplianceGate from src/lib/compliance.ts -->
<!-- Plan 01 creates that export. Since both are Wave 1, the gate wiring task must -->
<!-- account for the possibility that compliance.ts may not exist yet at test time. -->
<!-- Solution: The gate wiring is additive to route.ts -- if compliance.ts is not -->
<!-- yet built, the build will fail. Execute Plan 01 first or accept build-order dependency. -->

From src/app/api/generate-page/route.ts (existing):
```typescript
export async function POST(request: NextRequest): Promise<Response>
// Uses streamObject, returns result.toTextStreamResponse()
// Gate check goes AFTER streamObject completes (in onFinish) or as a post-generation validation
```

From axe-core (npm package, already installed):
```typescript
// axe.run(element, options): Promise<AxeResults>
// AxeResults.violations: Result[]
// Result: { id, impact, description, help, helpUrl, nodes: NodeResult[] }
// NodeResult: { html, failureSummary, target }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build axe-core scanner wrapper with tests</name>
  <files>src/lib/axe-scanner.ts, src/lib/__tests__/axe-scanner.test.ts</files>
  <behavior>
    - Test: scanForA11yViolations returns empty array for accessible HTML (div with proper text)
    - Test: scanForA11yViolations returns violation for img without alt attribute
    - Test: scanForA11yViolations returns violation objects with id, impact, description, help, helpUrl, nodes fields
    - Test: color-contrast rule is disabled in test environment (jsdom limitation)
  </behavior>
  <action>
    1. Create `src/lib/axe-scanner.ts` (per research Pattern 2):
       - Export `A11yViolation` interface: { id, impact, description, help, helpUrl, nodes: { html, failureSummary }[] }
       - Export `async function scanForA11yViolations(container: HTMLElement): Promise<A11yViolation[]>`
       - Dynamic import axe-core: `const axe = (await import("axe-core")).default`
       - Call `axe.run(container, { runOnly: { type: "tag", values: ["wcag2a", "wcag2aa"] } })`
       - Map `results.violations` to `A11yViolation[]`
       - This is browser-only. Never import this file in server-side code.

    2. Create `src/lib/__tests__/axe-scanner.test.ts`:
       - Tests run in jsdom environment (vitest config already sets this)
       - Create a container div, append to document.body, render test HTML
       - Test accessible HTML returns 0 violations
       - Test img without alt returns violation with id "image-alt"
       - Disable color-contrast rule in all tests (jsdom limitation per research Pitfall 1):
         Override axe config or add `rules: { "color-contrast": { enabled: false } }` before scanning
       - Clean up container after each test

    Run RED then GREEN: write tests first, then implement scanner.
  </action>
  <verify>
    <automated>cd /Users/ved/design-delivery-accelerator && npm test -- --run --testPathPattern=axe-scanner 2>&1 | tail -5</automated>
  </verify>
  <done>axe-scanner exports scanForA11yViolations and A11yViolation type. Tests verify accessible HTML passes and inaccessible HTML (missing alt) fails. Color-contrast disabled in jsdom tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire compliance gate into generate-page API route</name>
  <files>src/app/api/generate-page/route.ts</files>
  <action>
    Modify `src/app/api/generate-page/route.ts` to add a compliance gate check.

    Per research Pattern 3 and the "IMPORTANT NOTE on gating approach": the gate validates the COMPLETED PageSpec, not the streaming input. The correct architecture is:

    1. Add import: `import { runComplianceGate } from "@/lib/compliance";`

    2. In the `onFinish` callback (which already exists for audit logging), add gate validation:
       ```typescript
       onFinish: ({ object }) => {
         // Compliance gate: validate completed spec
         // Note: Since streaming is already complete at onFinish, we cannot
         // "block" the stream. Instead, we embed the gate result in the response.
         // The client (BuildUI) will check for compliance failures.

         // Existing audit logging (keep as-is)
         void logGeneration(...);
       }
       ```

    3. Since the stream has already been sent by onFinish, the TRUE gate enforcement happens client-side: BuildUI checks the compliance result before showing the render. To support this, modify the approach:
       - After `streamObject` returns the result, wrap the response handling:
       - Add a new endpoint behavior: if the query param `?gate=true` is passed, use `generateObject` (not `streamObject`) so the full spec is available for validation before responding.
       - For the hackathon demo, simpler approach: add `runComplianceGate` call in onFinish and store the result. The client-side ComplianceSidebar (Plan 03) will run the same check client-side for real-time feedback.

    SIMPLEST approach for hackathon (implement this):
    - Import `runComplianceGate` from `@/lib/compliance`
    - In the existing `onFinish` callback, after the object is available, run `runComplianceGate(object)` and log the result alongside the existing audit log
    - Add a comment noting that the client-side ComplianceSidebar provides the real-time gate UX (blocking render if violations exist)
    - This keeps the streaming UX intact while ensuring compliance is checked server-side for audit purposes

    Do NOT change the streaming behavior or break the existing useObject client-side integration.
  </action>
  <verify>
    <automated>cd /Users/ved/design-delivery-accelerator && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>generate-page route imports runComplianceGate, calls it in onFinish callback on the completed spec, and logs compliance result. Existing streaming behavior unchanged. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `npm test -- --run --testPathPattern=axe-scanner` -- all axe tests pass
- `npx tsc --noEmit` -- no type errors across the project
- axe-scanner returns typed A11yViolation array for DOM accessibility issues
- generate-page route calls runComplianceGate on completed spec in onFinish
</verification>

<success_criteria>
- scanForA11yViolations exported and tested with WCAG AA scanning
- A11yViolation type exported
- generate-page route has compliance gate check in onFinish
- No regression on existing streaming behavior or tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-comply-engine/03-02-SUMMARY.md`
</output>
