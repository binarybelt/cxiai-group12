---
phase: 02-build-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/components/page-renderer.tsx
  - src/components/build-ui.tsx
  - src/app/build/page.tsx
  - src/components/__tests__/page-renderer.test.tsx
autonomous: false
requirements: [BUILD-01, BUILD-04]

must_haves:
  truths:
    - "User can type a brief into a text area and click Generate to trigger the pipeline"
    - "Generated page appears in the preview panel as sections stream in"
    - "Preview renders actual React components from the component library (not mock HTML)"
    - "User can switch between two generated variants via tabs"
    - "Unknown componentIds are silently skipped without crashing the renderer"
  artifacts:
    - path: "src/components/page-renderer.tsx"
      provides: "Component registry renderer that maps PageSpec to React components"
      exports: ["PageRenderer"]
    - path: "src/components/build-ui.tsx"
      provides: "Client component with brief input, useObject streaming, variant tabs"
      exports: ["BuildUI"]
    - path: "src/app/build/page.tsx"
      provides: "Build page route at /build"
      exports: ["default"]
    - path: "src/components/__tests__/page-renderer.test.tsx"
      provides: "RTL tests for page renderer registry lookup"
  key_links:
    - from: "src/components/page-renderer.tsx"
      to: "src/components/index.ts"
      via: "imports all 12 components for COMPONENT_REGISTRY"
      pattern: "import.*from.*components/index"
    - from: "src/components/build-ui.tsx"
      to: "/api/interpret-brief"
      via: "fetch POST to interpret brief"
      pattern: "fetch.*api/interpret-brief"
    - from: "src/components/build-ui.tsx"
      to: "/api/generate-page"
      via: "useObject hook for streaming PageSpec"
      pattern: "useObject|api/generate-page"
    - from: "src/components/build-ui.tsx"
      to: "src/components/page-renderer.tsx"
      via: "renders PageRenderer with streamed spec"
      pattern: "PageRenderer"
---

<objective>
Build the page renderer (COMPONENT_REGISTRY pattern) and the Build UI that wires the entire pipeline together: brief input, interpret-brief fetch, streamed page generation via useObject, variant tabs, and live preview rendering with actual React components.

Purpose: This is the user-facing surface for BUILD-01 and BUILD-04. The user types a brief, gets a live-rendered page composed entirely from approved components, and can switch between two variants (Decision 13).

Output: Working /build page with end-to-end pipeline, passing renderer tests.
</objective>

<execution_context>
@/Users/ved/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ved/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-build-pipeline/02-RESEARCH.md
@.planning/phases/02-build-pipeline/02-01-SUMMARY.md
@.planning/phases/02-build-pipeline/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 and Plan 02 outputs -->

From src/types/page-spec.ts:
```typescript
export type ComponentRef = { componentId: string; props: Record<string, unknown>; tokenOverrides?: TokenOverride[] };
export type Section = { id: string; type: string; components: ComponentRef[]; order: number };
export type PageSpec = { id: string; title: string; market: string; product: string; sections: Section[]; metadata: PageSpecMetadata };
```

From src/agents/component-selector/schema.ts:
```typescript
// The schema wraps two variants:
// z.object({ variants: z.array(constrainedPageSpec).length(2) })
export function buildConstrainedPageSpecSchema(): ZodSchema;
```

From src/components/index.ts:
```typescript
export { Hero, Card, ISIBlock, Disclaimer, CTA, NavBar, Footer, DataTable, ClaimReference, SectionHeader, ContentBlock, ImageBlock };
```

API routes:
- POST /api/interpret-brief — { brief: string } -> BriefInterpretation
- POST /api/generate-page — BriefInterpretation -> streaming { variants: PageSpec[] }

Component prop shapes (node-type props accept JSON-friendly data at runtime):
- NavBar.links: NavLink[] = Array<{ label: string; href: string }>
- Footer.links: NavLink[], Footer.disclaimers: string[]
- DataTable.headers: string[], DataTable.rows: string[][]
- ISIBlock.content: string (ReactNode that accepts string)
- ContentBlock.body: string (ReactNode that accepts string)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for PageRenderer component</name>
  <files>src/components/__tests__/page-renderer.test.tsx</files>
  <behavior>
    - Test: PageRenderer renders a Hero component when spec contains componentId "Hero"
    - Test: PageRenderer renders a Card component with props passed through
    - Test: PageRenderer renders multiple sections in order (sorted by section.order)
    - Test: PageRenderer skips unknown componentId without crashing (renders nothing for that ref)
    - Test: PageRenderer handles empty sections array gracefully
    - Test: PageRenderer passes props from ComponentRef to the rendered component
    - Test: PageRenderer renders NavBar with links prop (NavLink[] structure passes through to component)
    - Test: PageRenderer renders DataTable with headers and rows props (string[][] structure passes through to component)
  </behavior>
  <action>
Write tests FIRST in `src/components/__tests__/page-renderer.test.tsx`. These tests MUST fail initially (RED phase) because PageRenderer does not exist yet.

Use Vitest + React Testing Library. Create mock PageSpec objects with known componentIds and verify they render:

1. Basic rendering tests:
   - Hero with `{ title: "Test Title", subtitle: "Sub" }` — verify "Test Title" appears in output
   - Card with `{ heading: "Card Heading" }` — verify "Card Heading" appears

2. Node-type prop tests (critical for structured props):
   - NavBar with `{ links: [{ label: "Home", href: "/" }, { label: "About", href: "/about" }] }` — verify NavBar renders and receives the links array (check "Home" and "About" text appear)
   - DataTable with `{ headers: ["Name", "Value"], rows: [["A", "1"], ["B", "2"]] }` — verify DataTable renders and receives the structured data (check "Name" and "A" text appear)

3. Edge case tests:
   - Unknown componentId "FakeWidget" is silently skipped (no crash, component count is correct)
   - Empty sections array renders without error
   - Sections render in order regardless of array position (section with order:2 after section with order:1)

Run tests to confirm they fail (RED). Do NOT implement PageRenderer in this task.
  </action>
  <verify>
    <automated>npm test -- --run src/components/__tests__/page-renderer.test.tsx 2>&1 | grep -E "FAIL|Tests|failed"</automated>
  </verify>
  <done>All 8 tests exist and FAIL because PageRenderer does not exist yet. Test file compiles. RED phase complete.</done>
</task>

<task type="auto">
  <name>Task 2: Implement PageRenderer, BuildUI, and /build page (GREEN phase)</name>
  <files>src/components/page-renderer.tsx, src/components/build-ui.tsx, src/app/build/page.tsx</files>
  <action>
1. Create `src/components/page-renderer.tsx`:
   - "use client" directive.
   - Import all 12 components from `@/components/index`.
   - Define `COMPONENT_REGISTRY: Record<string, React.ComponentType<Record<string, unknown>>>` mapping component ID strings to React components. The keys must exactly match the component IDs in components.json: "Hero", "Card", "ISIBlock", "Disclaimer", "CTA", "NavBar", "Footer", "DataTable", "ClaimReference", "SectionHeader", "ContentBlock", "ImageBlock".
   - `renderComponentRef` function: looks up component in registry, warns and returns null if not found, otherwise renders with spread props. Node-type props (NavBar.links, DataTable.headers/rows, Footer.links/disclaimers) are JSON-serializable and pass through as-is via spread — no special adapter needed since components accept these types directly.
   - `PageRenderer` component: receives `spec: Partial<PageSpec>` (partial because streaming may deliver incomplete data). Sort sections by order. Map each section's components through renderComponentRef.
   - Use optional chaining defensively: `spec.sections?.map(...)` to handle partial streaming state.

2. Create `src/components/build-ui.tsx`:
   - "use client" directive.
   - Import: `import { useObject } from "@ai-sdk/react"`. If this import fails at build time (package may still export it as `experimental_useObject`), use `import { experimental_useObject as useObject } from "@ai-sdk/react"` as fallback.
   - Two-phase submit handler:
     a. `fetch("/api/interpret-brief", ...)` with the brief string. Show "Interpreting..." state.
     b. On success, call `submit(interpretation)` on the useObject hook connected to `/api/generate-page`.
   - State: `brief` (textarea value), `interpretation` (stored for display), `selectedVariant` (0 or 1 for variant tabs).
   - Layout: Two-column grid. Left column: textarea for brief input, submit button, interpretation display (collapsible). Right column: variant tabs (A/B), PageRenderer with the selected variant's spec.
   - The useObject schema should match the server's output: `z.object({ variants: z.array(pageSpecSchema) })`. Import `buildConstrainedPageSpecSchema` for this.
   - Show loading states: "Interpreting brief..." and "Generating page..." at appropriate phases.
   - Show error state if either step fails.
   - Display the BriefInterpretation in a collapsible panel below the textarea (satisfies BUILD-02 "visible in the system").

3. Create `src/app/build/page.tsx`:
   - Simple page component that renders `<BuildUI />`.
   - Add a page title/header: "BUILD -- Design Delivery Accelerator".
   - This is a server component wrapper; BuildUI is the client component.

4. Run tests to verify all PageRenderer tests now pass (GREEN).
  </action>
  <verify>
    <automated>npm test -- --run src/components/__tests__/page-renderer.test.tsx && npm run build</automated>
  </verify>
  <done>All 8 PageRenderer tests pass (GREEN). PageRenderer renders approved components from COMPONENT_REGISTRY including node-type prop components (NavBar, DataTable), skips unknowns safely, handles partial streaming state. BuildUI connects the full pipeline: brief input -> interpretation display -> streamed variant generation -> live preview with variant tabs. Build page is accessible at /build. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify end-to-end BUILD pipeline with live LLM</name>
  <files>n/a</files>
  <action>Human verifies the complete BUILD pipeline works end-to-end with a real LLM call.</action>
  <what-built>
Complete BUILD pipeline: type a brief at /build, see interpretation, get two page variants rendered with actual pharma components.
  </what-built>
  <how-to-verify>
1. Set ANTHROPIC_API_KEY in .env.local
2. Run `npm run dev`
3. Navigate to http://localhost:3000/build
4. Enter a brief like: "Create an HCP landing page for Lipitor in the US market with efficacy data and safety information"
5. Click Generate
6. Verify:
   - Interpretation panel shows structured requirements (pageType, market, audience, etc.)
   - Preview panel shows a page building section by section as it streams
   - Page uses actual pharma components (Hero, ISIBlock, Footer, etc.) not placeholder HTML
   - Two variant tabs (A/B) appear and switching shows different page compositions
   - No console errors about unknown componentIds
7. Try a second brief to verify the pipeline resets cleanly
  </how-to-verify>
  <verify>Human visual inspection at http://localhost:3000/build</verify>
  <done>User confirms the BUILD pipeline generates a page from a brief using approved components with two variant options.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm test -- --run src/components/__tests__/page-renderer.test.tsx` -- all renderer tests pass
- `npm test -- --run src/agents/` -- all schema and route tests still pass
- `npm run build` -- full build succeeds with /build route
- Human verification: end-to-end pipeline works from brief to rendered page
</verification>

<success_criteria>
- User can type a brief at /build and see a live-rendered page appear (BUILD-01)
- Preview uses actual React components from the component library (BUILD-04)
- Two variant tabs allow switching between generated options (Decision 13)
- Interpretation is visible in a collapsible panel (BUILD-02)
- Unknown componentIds are handled gracefully (BUILD-03 defense-in-depth)
- Node-type prop components (NavBar with links, DataTable with headers/rows) render correctly
- All tests pass and build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-build-pipeline/02-03-SUMMARY.md`
</output>
